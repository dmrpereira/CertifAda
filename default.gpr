project Default is

   package Compiler is
      for Default_Switches ("ada") use ("-O2", "-gnat12", "-gnata");
   end Compiler;

   package Ide is
      for Vcs_Kind use "Git";
   end Ide;

   for Main use ("main.adb");

   package Builder is
      for Default_Switches ("ada") use ("-j2");
   end Builder;

end Default;

