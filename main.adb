with Ada.Text_IO; use Ada.Text_IO;
--with Virtual_Machine; 
--with Instruction_Spec; use Instruction_Spec;
--with Arith_Expr_Spec; use Arith_Expr_Spec;


procedure Main is
   package PrintBool is new Enumeration_IO(Boolean);

--   function Compile (E : in Arith_Exp) return Compiled_Program is
--      X : Compiled_Program(0.. 100);
--   begin
--      return X(0) := ;
--   end Compile;
  
begin
  null;
end Main;
